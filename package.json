{
  "name": "water-base",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run check:nav",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "check:dates": "node scripts/check-dates.js",
    "check:dashboard": "node scripts/check-dashboard.js",
    "check": "npm run check:dates && npm run check:dashboard && npm run check:notifications && npm run check:orders && npm run check:deposits && npm run check:readiness && npm run check:paperwork && npm run check:documents && npm run check:pipeline && npm run check:styles && npm run check:functions && npm run check:templates && npm run check:profit && npm run check:search && npm run check:checklist && npm run check:warranty && npm run check:embeds && npm run check:outstanding && npm run check:attention && npm run check:nav && npm run check:quote",
    "check:notifications": "node scripts/check-notifications.js",
    "check:orders": "node scripts/check-orders.js",
    "check:deposits": "node scripts/check-deposits.js",
    "check:readiness": "node scripts/check-readiness.js",
    "check:functions": "node scripts/check-functions.js",
    "check:documents": "node scripts/check-documents.js",
    "check:paperwork": "node scripts/check-paperwork.js",
    "check:pipeline": "node scripts/check-pipeline.js",
    "check:styles": "node scripts/check-styles.js",
    "check:templates": "node scripts/check-templates.js",
    "check:warranty": "node scripts/check-warranty.js",
    "check:embeds": "node scripts/check-embeds.js",
    "check:profit": "node scripts/check-profit.js",
    "check:search": "node scripts/check-search.js",
    "check:outstanding": "node scripts/check-outstanding.js",
    "check:attention": "node scripts/check-attention.js",
    "check:nav": "node scripts/check-nav.js",
    "check:quote": "node scripts/check-quote.js",
    "check:checklist": "node scripts/check-checklist.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "vite": "^8.1.1"
  }
}
